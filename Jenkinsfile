pipeline {
    agent any
        environment {
            NAME = 'Rak'
        }
        stages {
            stage('Build') {
                steps {
                    echo "$NAME"
                }
            }
        }
    }
